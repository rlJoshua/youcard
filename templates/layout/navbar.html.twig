<nav class="navbar navbar-expand-lg navbar-light bg-dark">
    <a class="navbar-brand" style="color: white" href="{{ path('home') }}">YouCard</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
            aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li  class="nav-item active">
                <a class="nav-link" style="color: white" href="{{ path('list_cards') }}">Liste de carte</a>
            </li>
            <li  class="nav-item active">
                <a class="nav-link" style="color: white" href="{{ path('add_card') }}">Ajouter une carte</a>
            </li>
            <li  class="nav-item active">
                <a class="nav-link" style="color: white" href="{{ path('list_factions') }}">Liste de faction</a>
            </li>
            <li  class="nav-item active">
                <a class="nav-link" style="color: white" href="{{ path('add_faction') }}">Ajouter une faction</a>
            </li>
            <li  class="nav-item active">
                <a class="nav-link" style="color: white" href="{{ path('list_decks') }}">Liste de Decks</a>
            </li>
            <li  class="nav-item active">
                <a class="nav-link" style="color: white" href="{{ path('add_deck') }}">Ajouter un Deck</a>
            </li>
        </ul>
        <span style="color: white" class="navbar-text">
            {% if app.user %}
                {{ app.user.getLastName }} {{ app.user.getFirstName }}
            {% endif %}
        </span>
    </div>
</nav>
{% for message in app.flashes('notification') %}
    <div class="flash-notif" style="background-color: darkcyan; padding: 15px 15px 15px 20px;width: 100%">
        {{ message }}
    </div>
{% endfor %}